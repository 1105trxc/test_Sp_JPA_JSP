package com.sgraphwebsite.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SiteMesh2Config {
    // SiteMesh disabled due to Jakarta (Boot 3) incompatibility.
}